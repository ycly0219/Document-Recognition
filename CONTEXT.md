# 领域模型 Glossary

## 核心概念

- **单据模板（Document Template）**：决定识别字段与导出结构的一类单据。当前包括 GE-ORACLE 拣货单、GE-OSCAR 拣货单、GE-发票单。
- **识别结果（Recognition Result）**：OCR 服务对单个文件返回的结构化字段，包含表头字段与明细字段。
- **明细行（Detail Line）**：识别结果中的一行物料/商品明细，是导出表格的基本行；发票拆分场景中，拆分后的子行才是导出基本行，预览汇总行不导出。
- **拆分汇总行（Split Summary Row / 明细汇总栏）**：发票原始明细拆分成多条子行时，预览表格按原始 QTY 保留一行汇总，仅展示物料编码、数量和“原始行汇总”提示；仅供预览核对，不进入复制与导出；其下子行向内缩进并按 `N.1`、`N.2` 编号。
- **拆分子行（Split Child Line）**：发票原始明细按 LPN/Serial 与 QTY 关系拆分后生成的可编辑明细行，是预览核对和 Excel 导出实际使用的行。
- **单条拆分（Single-child Split）**：发票原始明细只产生一条拆分子行的场景，典型为 QTY=1；该子行直接作为普通明细行展示与使用，不显示拆分汇总行。
- **插入明细行（Insert Detail Line）**：在选中的明细行下方插入一条空白明细行，用于人工补充遗漏项。
- **整行复制/粘贴（Row Copy/Paste）**：将选中明细行的单元格值作为一个整体复制，并在指定位置插入新的明细行后写入复制内容；与单元格复制无关。
- **预览表格（Preview Table）**：主界面中展示解析结果、允许人工核对与修改的区域，由单据头预览区和明细预览区组成。
- **单据头预览区（Preview Header Area）**：单个文件页签顶部以多列可编辑表单展示的一组单据头字段；一个文件只展示一组，支持修改并同步用于明细行的导出。
- **单据头多列表单（Header Form）**：不依赖横向滚动、按多行多列排列的单据头编辑表单；字段编辑时立即同步到当前文件的导出数据。
- **明细预览区（Preview Detail Area）**：单个文件页签底部展示的 Details 字段表格；承载明细行的编辑、新增、删除、复制与粘贴操作。
- **明细宽度自适应（Detail Column Fill）**：明细表格在横向空间有富余时自动均分列宽撑满界面；字段较多导致内容超宽时保留横向滚动。
- **处理进度（Processing Progress）**：从用户点击开始处理到预览表格展示解析结果之间的运行状态，进度完成表示预览已就绪。
- **处理日志（Processing Log）**：程序一次运行期间产生的批量处理、续查、导出等过程信息，供现场人员核对任务进展；与导出文件中的 `处理日志` Sheet 不是同一内容。
- **查询日志（Query Log）**：查看当前运行期间处理日志的动作/入口；默认不占用明细预览空间。
- **禁用态按钮（Disabled Button）**：当前阶段可见但不能点击的操作按钮；例如初始状态下「确认并导出」和「中止」，用于让用户知道功能存在但尚未满足使用条件。
- **继续查询原任务（Continue Query）**：对“结果未生成”文件按原 `reqUuid` 再次轮询 OCR 结果，不重新上传、不重新提交；可被「中止」停止并再次续查。
- **重新处理（Restart Processing）**：用户再次选择文件并开始处理新批次时，先清除上一批预览内容，再进入新的处理流程。
- **中止处理（Cancel Processing）**：用户主动放弃当前批量处理或继续查询过程并恢复到可用状态；批量中止不进入预览或导出，续查中止保留原待查询页签和 `reqUuid`，可再次续查。
- **确认（Confirm）**：用户人工核对预览表格内容，并认可结果后才允许导出的动作。
- **导出（Export）**：将核对后的明细行写入 Excel 文件的过程。
- **导出目录（Export Directory）**：人工指定的 Excel 文件目标文件夹；每批次导出的文件写入该文件夹，并可作为下次默认位置。
- **上次导出目录（Last Export Directory）**：最近一次人工选择并用于导出的目标文件夹；下次导出选择框默认打开该目录。
- **飞书统计同步（Feishu Statistics Sync）**：将当前批次统计记录写入飞书多维表格的过程；与单项文件的 OCR 识别和人工导出相互独立。
- **模拟数据（Mock Data）**：演示/验证预览表格时使用的样例数据，结构与识别结果一致，但不由真实接口调用产生；仅用于开发和测试阶段。
- **批量批次（Batch）**：单次选择并处理的一组输入文件，单次最多 5 个文件。
- **文件页签（File Tab）**：预览区域中对应单个输入文件的独立可编辑表格页签，失败文件也保留页签。
- **当前预览文件（Active Preview File）**：当前选中的文件页签对应的文件名和处理状态，界面会同步展示。
- **窗口自适应启动（Maximized Startup）**：程序启动时默认按当前系统可用的最大工作区显示，手动还原后仍保持接近最大尺寸。
- **单文件导出（Per-File Export）**：确认导出时为每个有明细的文件单独生成一个 Excel；无明细文件跳过。
- **采购订单表头模板（PO Header Template）**：`GE-发票单` 导出时复用的 Excel 结构，用于生成采购订单表头文件。
- **销售订单表头模板（Sales Order Header Template）**：`GE-ORACLE拣货单` 和 `GE-OSCAR拣货单` 导出时复用的 Excel 结构，用于生成销售订单表头文件。
- **固定值（Fixed Value）**：模板导出时按模板写入的常量业务值，例如仓库编号、货主、单位、质量状态；三种单据的订单类型均由单据头下拉选择，不再作为固定值写入。
- **订单类型（PO Type）**：三种单据共用的单据头必填字段。`GE-ORACLE拣货单` 与 `GE-OSCAR拣货单` 默认空，按“国内出库_FE”等 9 项中文选择，导出为 `GNCK_*` / `GWCK_*`；`GE-发票单` 默认“国外入库”，另支持“国内采购入库”“国内外维修入库”，导出为 `OSI` / `POIN` / `REPAIRIN`。
- **Flux WMS putPurchaseOrder**：内部 WMS 采购订单接收接口；当前发票功能使用用户提供的 QAS 测试 URL，固定货主 `GEHC` 与仓库 `WH004078`。
- **接口发送（WMS Send）**：发票页签顶部的发送入口；点击后展示当前页签组装好的只读 JSON 报文，确认发送后在同一窗口展示接口回告，并保留重新发送当前报文的入口；打开接口发送二级窗口时最小化程序，恢复主窗口时二级窗口会一起恢复。
- **putPurchaseOrder 报文（WMS Request Payload）**：由当前发票页签单据头与全部可编辑明细构成；头部仅含 `warehouseId`、`customerId`、`poType`、`docNo`、`udf01`、`udf02`，明细仅含映射字段、按发送顺序生成的 `lineNo` 与 `packUom=EA`，不发送原始汇总行。
- **接口回告（WMS Response）**：WMS 对已发送报文返回的处理结果，包含整体成功/失败状态与可人工查看的明细信息。
- **接口发送成功（WMS Send Success）**：WMS 回告明确确认本次报文处理成功的结果；只有接口成功标志为真时才算发送成功，明细级提示不影响该整体结果。
- **接口发送失败（WMS Send Failure）**：WMS 未明确确认本次报文处理成功的结果；界面保留重新发送入口，供用户按同一报文再次发送。
- **重新发送（Resend）**：对当前已展示报文再次执行接口发送的动作；无论上次发送成功或失败均可重复执行，重复执行不会造成额外副作用。
- **udf01（CARRIER）**：putPurchaseOrder 头部扩展字段，存放发票单头 `CARRIER` 值。
- **udf02（HAWB）**：putPurchaseOrder 头部扩展字段，存放发票单头 `HAWB` 值。
- **货物来源（Goods Source）**：GE-ORACLE 拣货单明细行上的来源标识；`GE-发票单` 在订单类型为“国外入库”时同样固定为 `ORACLE`。
- **参考编号3（Reference #3）**：销售订单模板中的第三个参考编号；GE-ORACLE 拣货单场景对应接口的系统编号。
- **质量状态（Quality Status）**：GE-ORACLE 拣货单物料的质量分类；按仓库子库后缀识别为 `GOOD` 或 `BAD`。
- **好件（Good Unit）**：GE-OSCAR 拣货单中可正常发货的物料状态；导出质量状态时写入 `GOOD`，非好件状态导出为空。
- **LPN**：GE-ORACLE 拣货单明细中的物流容器/托盘编号；在物料明细信息中必有值。
- **Serial**：GE-ORACLE 拣货单明细中的物料序列号；在物料明细信息中可能缺失。
- **Lot**：GE-ORACLE 拣货单明细中的物料批号；在物料明细信息中可能缺失。
- **COO**：GE-ORACLE 拣货单明细中的原产地（Country of Origin）；在物料明细信息中可能缺失。
- **收货地址（Ship To Address）**：GE-ORACLE 拣货单中的收货方地址，属于单据头部收货方信息。
- **SHIP TO NO（收货方编号）**：GE-ORACLE 拣货单中由 OCR 识别结果提供的收货方编号，与收货地址共同构成收货方信息。
- **UN Number**：GE-ORACLE 拣货单物料明细信息中可能出现的危险品编号，不进入导出的追溯标识字段。
- **物料明细信息（Item Details）**：GE-ORACLE 拣货单明细中携带 LPN、Serial、Lot、COO 的标识文本；有值的字段有且仅有一个值。
- **空白预渲染模版（Pre-rendered Blank Template）**：选择单据模板后立即生成的空白可编辑单据页签，包含空单据头和空明细，用于不依赖 OCR 的人工填写或 OCR 失败时接管。
- **人工填写（Manual Entry）**：用户绕过 OCR 结果，直接在空白预渲染模版、失败页签或“结果未生成”页签中录入并导出数据的过程。
- **手工导出名（Manual Export Name）**：无文件手工空白页导出时的输出文件基准名；按模板单据编号字段取值，缺失时回退为“空白单据”。
- **OCR结果覆盖（OCR Result Overwrite）**：对“结果未生成”页签继续查询并成功时，以新识别结果重建页签的行为；即使页签中已有人工填写内容也直接覆盖。
